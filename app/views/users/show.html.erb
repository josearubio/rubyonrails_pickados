<% provide(:title, @user.name) %>
<div class="container"><br><br><br>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
    <section class="stats">
      <%= render 'shared/stats' %>
    </section>

    <section class="userstats">
      <% if @user.stats.any? %>
      <div class="panel panel-default">

          <div class="panel-heading">
            <h3 class="panel-title"><span class="glyphicon glyphicon-stats"></span> Estadísticas
              <span class="glyphicon glyphicon-calendar"></span> <%= l(@user.stats.last.created_at, format: :mesano)%></h3>
          </div>
        <div class="panel-body">

              <%= render 'shared/userstats' %>

        </div></div>
      <% end %>
    </section>
  </aside>
  <div class="col-md-8">
    <%= render 'follow_form' if logged_in? %>
    <% if @user.picks.any? %>
        <h3>Picks <span class="glyphicon glyphicon-tasks"></span></h3>
        <div class="panel panel-default">
          <div class="panel-body">
        <ol class="picks">
          <%= render @picks %>
        </ol>
            </div></div>
        <%= will_paginate @picks %>
    <% end %>
  </div>
</div></div>